{
  "name": "pi-sidebar",
  "version": "0.1.0",
  "description": "Herdr-pane telemetry panel for the Pi coding agent: statusline-parity context, cost, tokens, model and git, plus a pi-plugin-dev skill HUD.",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "author": "mastnacek",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mastnacek/pi-sidebar.git"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-coding-agent",
    "pi-extension",
    "sidebar",
    "herdr",
    "pane",
    "statusline",
    "telemetry"
  ],
  "files": [
    "src",
    "index.ts",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "tsc && node --test dist/test/**/*.test.js"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@types/node": "^22.13.0",
    "typebox": "^1.3.19",
    "typescript": "^5.8.0"
  }
}
